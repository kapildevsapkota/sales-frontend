import PackagingLayout from "@/components/layout/dashboard/PackagingLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <PackagingLayout>{children}</PackagingLayout>;
}
